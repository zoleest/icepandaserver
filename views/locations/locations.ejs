<!DOCTYPE html>
    <html>
        <%- include('../partials/head'); %>
      <body>
        <div id="content" class="container-fluid d-flex flex-column mx-0 px-0" >
              <div class="row px-2"><%- include('../partials/navbar'); %></div>
        </div>
        <div id="location-wrapper" class="container-fluid bg-dark bg-gradient">
            <h1 class="text-light text-center pt-3"><%=language.locations.locations%></h1>
            <% for(let locationCounter = 0; locationCounter < locationData.length; locationCounter++){

                if(locationCounter % 4 === 0){

                    %>
                        <div class="row justify-content-center">
                   <%

                }

            %>

                <div class="col-12 col-md-6 col-lg-3">
                 <div class=" p-5 text-center text-light">
                   <a class="link link-success text-decoration-none" href="/locations?id=<%=locationData[locationCounter].location_slug%>">
                        <div class="ratio ratio-4x3">
                              <img src="/images/locations/<%=locationData[locationCounter].location_slug%>.webp">
                        </div>
                         <h3 class="mt-2"><%=locationData[locationCounter].location_name%><h3>
                    </a>
                   </div>

                </div>


            <%

                    if(locationCounter % 4 === 3){

                                        %>
                                            </div>
                                        <%

                                    }

            } %>
            </div>
      </body>
   </html>
